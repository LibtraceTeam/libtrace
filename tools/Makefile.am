# tracedump depends on libpacketdump
TRACEDUMP_DIR=tracepktdump

SUBDIRS=traceanon tracemerge tracesplit $(TRACEDUMP_DIR) tracertstats tracestats 
SUBDIRS+=tracereport tracetop tracereplay tracediff traceends

